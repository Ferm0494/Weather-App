const container = document.getElementsByClassName('weather-component')[0]
const spinner = document.getElementsByClassName('spinner')[0]
const values = document.getElementsByClassName('values')
const gifImg = document.getElementById('glify')
const form = document.getElementsByTagName('input')[0]
const backgroundContainers = document.getElementsByClassName('backg')
export {
    backgroundContainers,
    form,
    container,
    spinner,
    values,
    gifImg
}